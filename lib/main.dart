import 'package:flutter/material.dart';

void main() {
  runApp(const BreezeForecast());
}

class BreezeForecast extends StatelessWidget {
  const BreezeForecast({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp();
  }
}
